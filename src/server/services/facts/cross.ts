/**
 * F3 — `crossAnalyze`, as its own importable module.
 *
 * Real implementation in `code-analyzer.ts` (the "F3" section) — see
 * `facts/types.ts`'s docstring for why this is a re-export.
 */
export { crossAnalyze } from "../code-analyzer.js";
export type { CrossInput } from "../code-analyzer.js";
